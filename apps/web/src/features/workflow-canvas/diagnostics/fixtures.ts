import { createCanvasPerformanceFixture } from '../utils/performance-fixture'

export const workflowCanvasPerformanceFixtures = {
  nodes20: createCanvasPerformanceFixture(20),
  nodes100: createCanvasPerformanceFixture(100),
  nodes500: createCanvasPerformanceFixture(500),
}
